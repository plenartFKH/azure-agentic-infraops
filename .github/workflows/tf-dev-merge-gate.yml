name: tf-dev Merge Gate

on:
  pull_request:
    branches:
      - main

# Terraform support is fully integrated — phase gate retired.
# Workflow kept as a required status check placeholder.
jobs:
  tf-dev-complete:
    name: Terraform Support Complete
    runs-on: ubuntu-latest
    steps:
      - name: Gate retired — Terraform support fully integrated
        run: echo "All Terraform phases complete. Gate retired."
